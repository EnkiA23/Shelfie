# Core runtime. Everything here is small and pure-wheel, so a clean install is
# under a minute. The YOLO detector lives in requirements-detector.txt because it
# pulls torch (~2 GB); without it the detector falls back to OpenCV, which is a
# supported path rather than a broken one.
django>=5.0,<6.0
djangorestframework>=3.15
django-cors-headers>=4.3
django-environ>=0.11
rapidfuzz>=3.6
pillow>=12.3.0
opencv-python-headless>=4.9
